<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
          background-image:linear-gradient(to right,#f24e64,#f78a98);
        }
        .fade-element {
          opacity: 1;
          animation-name: fadeOut;
          animation-duration: 3s;
          animation-fill-mode: forwards;
        }
        @keyframes fadeOut {
          0% { opacity: 1;background-color: rgba(102, 205, 102, 0.851); }
          100% { opacity: 0;background-color: whitesmoke; }
        }
        .btn{
        margin-left: 20px;
          font-weight: 650;
          background-color: #e0553d;
          border: 1px solid #d5d9d9;
          border-radius: 8px;
          box-shadow: rgba(213, 217, 217, .5) 0 2px 5px 0;
          box-sizing: border-box;
          color: #0f1111;
          cursor: pointer;
          display: inline-block;
          font-family: "Amazon Ember",sans-serif;
          font-size: 13px;
          line-height: 29px;
          padding: 0 10px 0 8px;
          position: relative; 
          text-align: center;
          text-decoration: none;
          user-select: none;
          -webkit-user-select: none;
          touch-action: manipulation;
          vertical-align: middle;
          width: 100px;
          font-weight: 600;
      }
        </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
    <h6 class="fade-element"><%=msg%></h6>
    <nav id="navbar-example2" class="navbar  px-3 mb-3">
        <h3 class="navbar-brand">MobiDoc</h3>
        <h5 aria-expanded="false"><%=k%></h5>
      </nav>
      <form action="/closed" method="get">
        <button class="btn" type="submit" name="button" value="closed">Closed</button>
        <button class="btn" type="submit" name="button" value="open">Open</button>
      </form>
      <form action="/account1">
      <h6>To View All patients visited</h6>
      <button class="btn">Browse</button>
    </form>
</body>
</html>